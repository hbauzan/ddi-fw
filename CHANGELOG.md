# Changelog

## Unreleased

- Path `measure_and_save` / `--no-prune`: persiste mazos completos bajo `ddi_fw/out/qwen2/` sin `podar_hasta_publicar` y sin pisar el `rows.npz` de BGE-M3.
- Ola Q: smoke live de `Alibaba-NLP/gte-Qwen2-1.5B-instruct` = `blocker_load` (`custom_code` / `rope_theta` vs transformers 5.17). Pin de producto sin cambios: `BAAI/bge-m3`.

## 0.1.0 — 2026-09-19

- Pack fundacional: almas `python` / `legal` / `receta`, hoja + corte duro, CLI press, ingress fail-closed, egreso hold, proxy OpenAI-compatible, seam multi-embedder.
